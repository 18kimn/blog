function insertLink(raw: string) {
  // match doi string
  // match https string
}
// receives: HTML string
// returns: HTML string, plus some additional styles
export function styleCitation(raw: string) {
  // does raw have a link or doi?
  //
  //
}
