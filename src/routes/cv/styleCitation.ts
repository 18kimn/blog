function _insertLink(_raw: string) {
  // match doi string
  // match https string
}
// receives: HTML string
// returns: HTML string, plus some additional styles
export function styleCitation(_raw: string) {
  // does raw have a link or doi?
  //
  //
}
